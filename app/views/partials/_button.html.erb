    <% if game.ongoing %>
      <%= link_to "Quit game", quit_game_path(game), method: :patch, class: "btn btn-cta", id: "btn-small" %>
    <% else %>
      <%= link_to "Play again!", user_path, class: "btn btn-cta" %>
    <% end %>
